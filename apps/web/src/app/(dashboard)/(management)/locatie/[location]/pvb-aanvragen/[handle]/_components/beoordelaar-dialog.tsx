"use client";

export function BeoordelaarDialog({
  open,
  onClose,
  params,
}: {
  open: boolean;
  onClose: () => void;
  params: Promise<{ location: string; handle: string }>;
}) {
  // TODO: Implement beoordelaar dialog
  return null;
}
