"use client";

export function StartTimeDialog({
  open,
  onClose,
  params,
}: {
  open: boolean;
  onClose: () => void;
  params: Promise<{ location: string; handle: string }>;
}) {
  // TODO: Implement start time dialog
  return null;
}
